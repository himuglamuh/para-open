A function in `src/sum.js` is supposed to return the inclusive sum of integers
from `1` to `n`. The test in `src/sum.test.js` pins the expected behavior and
is currently failing.

Find the bug, fix it with the smallest possible change, and explain in a few
sentences what was wrong. Do not rewrite unrelated code.

After you're done, run the test from `src/` (`node --test src/sum.test.js`) and
confirm it passes.
